"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.departmentReports = void 0;
// =============================================================================
// Dept Review V1 — department_reports (weekly per-department HR reports)
//   Mirrors `database/schema.sql §37` 1:1.
//
// One row per (space, Dhaka-calendar week). `payload` is the full stats
// snapshot computed at generation time (shape defined by ReportStatsService —
// P18); it is REGENERATABLE (upsert on the unique key updates payload/
// generated_* only — never head_note / acknowledged_* / notified_at).
//
// `notified_at` is the atomic one-time-fanout claim: after the upsert, the
// generator runs `UPDATE … SET notified_at = ? WHERE space_id = ? AND
// week_start = ? AND notified_at IS NULL` — affectedRows=1 elects exactly ONE
// notifier, closing the weekly-job-vs-manual-generate race AND the
// "manual generate before the cron suppresses the notification" hole.
//
// `generated_at`/`notified_at`/`acknowledged_at` are APP-WRITTEN UTC (no DB
// defaults — the M8 clock-domain rule). Week dates use string mode ("YYYY-MM-DD")
// deliberately: week keys are calendar identifiers computed by the Dhaka week
// helpers, never Date objects (avoids DATE↔timezone coercion).
//
// `head_user_id`/`generated_by`/`acknowledged_by` are point-in-time SNAPSHOTS
// (deliberately no user FKs — a later role change must not rewrite history).
// The space FK is RESTRICT: reports are retained HR history — a space with
// reports can be archived forever but never hard-deleted (`SpacesService`
// surfaces `409 space.has_reports`).
// =============================================================================
const mysql_core_1 = require("drizzle-orm/mysql-core");
const _shared_1 = require("./_shared");
const auth_1 = require("./auth");
const hierarchy_1 = require("./hierarchy");
exports.departmentReports = (0, mysql_core_1.mysqlTable)("department_reports", {
    id: (0, mysql_core_1.varchar)("id", { length: _shared_1.ID_LENGTH }).primaryKey(),
    // Keyset-cursor tie-break (composite with week_start) — never on the wire.
    internalId: (0, mysql_core_1.bigint)("internal_id", { mode: "bigint", unsigned: true })
        .notNull()
        .autoincrement(),
    workspaceId: (0, mysql_core_1.varchar)("workspace_id", { length: _shared_1.ID_LENGTH }).notNull(),
    spaceId: (0, mysql_core_1.varchar)("space_id", { length: _shared_1.ID_LENGTH }).notNull(),
    weekStart: (0, mysql_core_1.date)("week_start", { mode: "string" }).notNull(),
    weekEnd: (0, mysql_core_1.date)("week_end", { mode: "string" }).notNull(),
    /** Head snapshot at generation — NULL for headless departments (they still report). */
    headUserId: (0, mysql_core_1.varchar)("head_user_id", { length: _shared_1.ID_LENGTH }),
    headNote: (0, mysql_core_1.varchar)("head_note", { length: 1000 }),
    payload: (0, mysql_core_1.json)("payload").$type().notNull(),
    /** NULL = generated by the automated job; else the requesting user id (snapshot). */
    generatedBy: (0, mysql_core_1.varchar)("generated_by", { length: _shared_1.ID_LENGTH }),
    generatedAt: (0, mysql_core_1.timestamp)("generated_at").notNull(),
    notifiedAt: (0, mysql_core_1.timestamp)("notified_at"),
    acknowledgedBy: (0, mysql_core_1.varchar)("acknowledged_by", { length: _shared_1.ID_LENGTH }),
    acknowledgedAt: (0, mysql_core_1.timestamp)("acknowledged_at"),
}, (t) => ({
    internalIdUq: (0, mysql_core_1.uniqueIndex)("uq_department_reports_internal_id").on(t.internalId),
    spaceWeekUq: (0, mysql_core_1.uniqueIndex)("uq_department_reports_space_week").on(t.spaceId, t.weekStart),
    wsFk: (0, mysql_core_1.foreignKey)({
        columns: [t.workspaceId],
        foreignColumns: [auth_1.workspaces.id],
        name: "fk_dept_reports_ws",
    })
        .onDelete("cascade")
        .onUpdate("cascade"),
    spaceFk: (0, mysql_core_1.foreignKey)({
        columns: [t.spaceId],
        foreignColumns: [hierarchy_1.spaces.id],
        name: "fk_dept_reports_space",
    })
        .onDelete("restrict")
        .onUpdate("cascade"),
    wsWeekIdx: (0, mysql_core_1.index)("idx_department_reports_ws_week").on(t.workspaceId, t.weekStart),
}));
