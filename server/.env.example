# Server
PORT=4000
NODE_ENV=dev

# JWT (must be a long random string)
SECRET_KEY=replace-with-a-long-random-secret

# MySQL
DB_HOST=localhost
DB_PORT=3306
DB_USERNAME=root
DB_PASSWORD=password
DB_NAME=task_management

# Frontend URL (for CORS + email links if needed)
FRONTEND_URL=http://localhost:5173
