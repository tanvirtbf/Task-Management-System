# Generated output only. Everything a person writes gets linted.
#
# This file used to hide 321 source files — the whole of `tests/` (312
# files), every operator script, and one jest config singled out for no
# reason anyone recorded. They were excluded because the TYPE-AWARE
# parser cannot resolve files outside `tsconfig.json`'s include and
# reports one parse error each; the fix for that is an `overrides` block
# with `project: null` (see .eslintrc.cjs), not an ignore list. An
# ignore entry does not say "this file is fine" — it says nobody will
# ever find out.
dist
node_modules
src/db/migrations
