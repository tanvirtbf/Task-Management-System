# F33 — Production parity re-check (Block H)

**Status:** DONE (local half) — the box-only half is a **documented operator handoff** (§4), which
is what P41 §9 already scoped it to; F33 re-validated that list is still exactly right.
**Fixes:** none by charter. **Runbook corrections: 3** (dated edits in `DEPLOY_READINESS_SCAN` §6).
**Gate:** the §6 runbook walk completed with every wrong step logged AND corrected in place.
**Evidence:** `fixing/evidence/F33/db-setup-fresh.txt` · the corrected scan §6 · this file.

---

## 1. `db:setup` on a fresh prod-shaped database — PASS, with a NEW canonical shape

```
DB_NAME_OVERRIDE=taskmanagement_f33 npm run db:setup:fresh
  Database setup complete {"tables":42,"views":5,"triggers":9}
```

**42 / 5 / 9 — not the historical 41 / 5 / 7, and that is correct.** The delta is the fixing
campaign folding its upgrades into `schema.sql` per rule X4:

| delta | origin |
|---|---|
| +1 table `r2_purge_queue` | F16 (hard-delete queues R2 keys; `upgrades/007`) |
| +2 triggers `trg_comments_after_update`, `trg_form_submissions_after_delete` | F15 (the counter rebuild; `upgrades/006`) |

Verified by enumerating the fresh DB's triggers/tables, not by trusting the log. The runbook's
§6.1.3 cited 41/7/5 and would have produced a **false alarm on deploy night** — corrected with a
dated note, and its "upgrades 001–004 not needed on fresh" widened to **001–013**. The scratch DB
was dropped after the check.

## 2. The production client bundle — cleaner than P41

Built by F30's config (`vite build`, production mode, manualChunks). Across all 45 JS chunks:
baked URL is the relative `"/api/v1"`; secret-name scan **0 matches**; real-key-shape scan
(`sk-[40+ chars]`) **0**; `mock-api` absent; and the dead `:5501` fallback string P41 could still
see in the bundle **no longer appears at all**.

## 3. The five deploy surfaces — all present, all current

The deploy/ tree post-dates the scan (the last five commits are its hardening) and F33 re-read each
against today's build shape:

- **nginx** (`tasks.beautybooth.com.bd.conf`): serves `client/dist` only; `/api/v1/` proxied with
  an **exact-match SSE location** for `/api/v1/assistant/chat` (`proxy_buffering off`);
  `client_max_body_size 32m` (covers the 30 MB attachment path); `/metrics` and `/health/ready`
  blocked from the internet; `/assets/` immutable + `index.html` no-cache — every §6.3 requirement,
  plus the Cloudflare real-IP recovery and the single 80+443 block from the deploy commits.
- **pm2**: `dist/server.js`, `NODE_ENV=prod`, 1 instance, 400M restart cap — matches §6.2/B1.
- **cron**: **all six jobs** via one `run-job.sh` (HTTP + `INTERNAL_JOB_TOKEN` from the env file,
  fail-fatal when empty, 300 s curl cap), staggered 02:10–02:40, snooze-wake every 5 min,
  department-report `0 3 * * 1` = **Monday 09:00 Dhaka on a UTC box** — the conversion the scan
  asked for. The scan's "form-submission-expiry has no HTTP route" is obsolete: the route exists
  (`routes/jobs.ts`), so the uniform HTTP approach is right.
- **logrotate**: covers both `server/logs/*.log` (winston, uncapped by default) and
  `/var/log/bbtasks/*.log` (cron output).
- **backup**: nightly `mysqldump --single-transaction`, 14-day retention, **free-disk abort guard**
  and a dump-size sanity check — closing §6.1.5's "nothing exists in the repo for backups", which
  is now marked CLOSED in the scan.

## 4. The §6 walk — what was wrong, now fixed in the document

1. **§6.1.3** — stale 41/7/5 → 42/5/9 with the X4 provenance (above).
2. **§6.1.5** — "no backups in the repo" → closed by `deploy/backup`.
3. **§6.2** — the required-env list **omitted `DB_TIMEZONE=+00:00`**, which F3 made a boot
   requirement (prod refuses to start without it) — the sharpest possible runbook bug: following
   the list verbatim produced a non-booting server. Added, with the MySQL-8.4 `DB_SOCKET_PATH`
   note for the real box and MEDIUM-2's TZ question marked settled by F3.

Rate limiters and prod security headers were P41-verified; the code that produces them changed in
F13 (after P41), and the **health module's `security-headers` suite (14/14, in the F32 sweep)** is
the current proof — cited rather than re-probed by hand.

## 5. The box-only handoff (unchanged in scope from P41 §9, re-confirmed still right)

Needs SSH to 209.38.65.61 / breaking live credentials — cannot be closed from this machine:

| item | first blocked at |
|---|---|
| walk §6 on the real box end-to-end | P41 |
| cookie `Secure`/`HttpOnly`/`SameSite` observed in real prod mode | P41 (limiter cooldown) |
| is TCP 5501 firewalled (ISS-089's deciding question) | P41 |
| R2 genuinely unreachable / `run-job.sh` with the API down | P23 / P32 |
| `eng.not_configured` on a live workspace | P29 |
| the 23:59→00:01 Dhaka boundary with the system clock moved | P19/P37 |
| assistant upstream failures (timeout / openai_error / empty_reply) | P33 |
| SPA-origin headers behind the real vhost | P38 |

Each is listed in the scan and in F34's summary as **operator work at first deploy**, with the
repo-side halves (routes, guards, scripts) already verified here.
