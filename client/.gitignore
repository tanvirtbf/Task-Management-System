# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
# `dist` is DELIBERATELY tracked — see the root .gitignore. The production box
# cannot build, so the bundle ships in git. Ignoring it here while it is tracked
# is what let a rebuild stage the new index.html and the deleted old assets
# without the new ones.
dist-ssr
*.local

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.env
.env.local
.env.*.local

# Playwright scratch output (P0 mobile net)
test-results/
playwright-report/
