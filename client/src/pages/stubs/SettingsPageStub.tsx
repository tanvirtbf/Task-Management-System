import { Settings } from "lucide-react";
import { ComingSoonPage } from "../../components/shared/ComingSoonPage";

const SettingsPageStub = () => (
    <ComingSoonPage
        icon={Settings}
        title="Settings"
        description="Profile, notifications, integrations, users, statuses, custom fields, webhooks, templates — all 17 settings sub-pages. Phase 10."
        phase="Phase 10"
    />
);

export default SettingsPageStub;
