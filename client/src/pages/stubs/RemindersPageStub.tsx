import { Clock } from "lucide-react";
import { ComingSoonPage } from "../../components/shared/ComingSoonPage";

const RemindersPageStub = () => (
    <ComingSoonPage
        icon={Clock}
        title="Reminders"
        description="Personal action items separate from tasks — delegate, recur, snooze. Phase 11."
        phase="Phase 11"
    />
);

export default RemindersPageStub;
