import { Search } from "lucide-react";
import { ComingSoonPage } from "../../components/shared/ComingSoonPage";

const SearchPageStub = () => (
    <ComingSoonPage
        icon={Search}
        title="Search"
        description="Global search across tasks, comments, docs, notes — with filters and saved searches. Phase 11."
        phase="Phase 11"
    />
);

export default SearchPageStub;
