import { StickyNote } from "lucide-react";
import { ComingSoonPage } from "../../components/shared/ComingSoonPage";

const NotepadPageStub = () => (
    <ComingSoonPage
        icon={StickyNote}
        title="Notepad"
        description="Personal rich-text notes with auto-save and convert-to-task. Phase 11."
        phase="Phase 11"
    />
);

export default NotepadPageStub;
